+ Run service docker

    docker-compose up --build

    *up: Starts the container
    *--build: Re-compiles your TypeScript into JavaScript inside the container

+ Stop docker runing

    docker-compose down

    *If you want to stop the containers AND remove the internal networks Docker created for your API

+ Vertify docker images isStop

    docker ps